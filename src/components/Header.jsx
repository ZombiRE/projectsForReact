import React from "react";

export default function Header(){

    return(
        <header>
            <div>
                <span className="logo">Магазин IP-телефонов</span>
            </div>
            <div className="presentation"></div>
        </header>
    );
}